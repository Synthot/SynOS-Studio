id: developer
extends: workstation
description: Workstation plus containers and toolchains, no policy lockdown.
software:
  bundles: [containers, build-tools]
  packages:
    add: [git, code-oss, python3-venv]
installer:
  passwordless_sudo: true
security:
  usbguard: false
