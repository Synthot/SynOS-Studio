#!/bin/bash
# synos-installer-beta — GTK4 native installer for SynOS (beta)
#
# The UI remains unprivileged to preserve the desktop session. It sends one
# validated, immutable plan to the separate root executor when installation
# begins.
exec /usr/bin/python3 /usr/lib/synos-installer-beta/main.py "$@"
