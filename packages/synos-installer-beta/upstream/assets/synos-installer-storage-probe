#!/bin/sh
set -eu
if [ "$#" -ne 1 ] && [ "$#" -ne 2 ]; then
    echo "The storage probe accepts one disk path or an NTFS inspection." >&2
    exit 2
fi
exec /usr/bin/python3 /usr/lib/synos-installer-beta/storage_probe_cli.py "$@"
