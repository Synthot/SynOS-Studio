container_services: []            # [{name, image, ports, volumes, env, gpu, cap_add, description}]
container_services_gpu: none      # none | nvidia | amd | intel: how gpu: true services reach the GPU
