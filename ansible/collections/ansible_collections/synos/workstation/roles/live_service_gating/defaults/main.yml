live_service_gating_packages: []      # concrete software.packages.add, resolved across the whole extends chain (tools/render_manifest.py's resolved["packages"]["appliance"]) — never a curated profiles/bundles.yml group
