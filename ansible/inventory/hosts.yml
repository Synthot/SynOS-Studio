# Example inventory for run-time management.
all:
  children:
    workstations:
      hosts:
        ws-fin-001.corp.acme.example:
      vars:
        ansible_user: it-admin
        ansible_become: true
